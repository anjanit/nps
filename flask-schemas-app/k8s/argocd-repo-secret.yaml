apiVersion: v1
kind: Secret
metadata:
  name: private-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  url: git@github.com:anjanit/JOBCHANGE.git
  sshPrivateKey: |
    # This will be replaced with your actual SSH key
  insecure: "true" # Skip host key verification (not recommended for production)
